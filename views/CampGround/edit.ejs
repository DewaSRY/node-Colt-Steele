<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta
      name="viewport"
      content="width=
    , initial-scale=1.0"
    />
    <title>Edit Camp Ground</title>
  </head>
  <body>
    <form
      action="/camp-ground/<%= campGroundDetail._id %>?_method=PUT"
      method="POST"
    >
      <div>
        <label for="title"> title</label>
        <input type="text" id="title" / name="campGround[title]" value="<%=
        campGroundDetail.title %>">
      </div>
      <div>
        <label for="location"> location</label>
        <input type="text" id="location" / name="campGround[location]"
        value="<%= campGroundDetail.location %>">
      </div>
      <button type="submit">Edit camo ground</button>
    </form>
    <p>
      <a href="/camp-ground/<%= campGroundDetail._id %>">
        back to Camp Ground</a
      >
    </p>
  </body>
</html>
